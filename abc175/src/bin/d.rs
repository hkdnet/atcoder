fn build_loop(n: usize, ps: [usize]) -> Vec<Vec<usize>> {
    let mut ret = Vec::new();
    let mut total = 0;

    ret
}
fn main() {
    use proconio::input;
    input!(n: usize, k: u32);
    input!(ps: [usize; n]);
    input!(cs: [i64; n]);
}
