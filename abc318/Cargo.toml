[package]
name = "abc318"
version = "0.1.0"
authors = ["GitHub <noreply@github.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"

[profile.release]
lto = true
panic = 'abort'
